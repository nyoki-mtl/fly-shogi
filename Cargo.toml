[package]
name = "fly-shogi-lab"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
publish = false

[dependencies]
rsshogi = { version = "=1.2.4", features = ["policy-labels", "records"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
